engine-strict=true
fund=false
save-exact=true
message=🔖 %s
tag-version-prefix=
# The supply-chain cooldown lives in .github/dependabot.yml. npm's own
# `min-release-age` is not set here: it breaks Dependabot and does nothing for `npm ci`.
